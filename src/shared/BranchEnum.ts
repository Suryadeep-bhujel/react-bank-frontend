// Temporary branch enum to resolve build issues
export enum BranchStatus {
    ACTIVE = "ACTIVE",
    INACTIVE = "INACTIVE"
}

export enum BranchType {
    MAIN = "MAIN",
    SUB = "SUB",
    EXTENSION = "EXTENSION"
}